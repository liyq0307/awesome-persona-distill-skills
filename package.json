{
  "name": "awesome-persona-distill-skills",
  "version": "0.1.0",
  "private": true,
  "description": "Persona distillation skills for turning people, relationships, and personal context into reusable AI assistants.",
  "license": "CC0-1.0",
  "packageManager": "bun@1.3.11",
  "engines": {
    "bun": ">=1.2.0"
  },
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "bun test",
    "check:consistency": "bun scripts/check-repository-consistency.mjs",
    "check:links": "bun scripts/check-links.mjs"
  },
  "devDependencies": {
    "prettier": "^3.8.1"
  }
}
